# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Taxi' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
 pod 'UberRides', '~> 0.7'
  pod 'OAuthSwift'
pod 'SwiftyTimer'
  # Pods for Taxi

  target 'TaxiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TaxiUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
